
    <?php
            foreach ($lists as $l):
                $memo = !empty($l['bres_memo']) ? json_decode($l['bres_memo'], true) : [];
                if (empty($l['bres_result'])) {
                    $nums = array_fill(0, 5, '?');
                } else {
                    $nums = explode(',', $l['bres_result']);
                }
        ?>
    <li class="item flex">
        <div class="flex1">
            <p class="tit"><?=$l['bres_periods'];?></p>
            <dl class="prize-data flex">
                <dd class="text flex1">
                    <?php foreach ($nums as $i => $n): $ruleIdx = $i+2;?>
                    <span>
                        <i class="circle"><?=$n;?></i>
                    </span>
                    <?php endforeach; ?>
                </dd>
            </dl>
        </div>
        </div>
    </li>
    <?php endforeach; ?>
